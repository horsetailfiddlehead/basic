# files to globally ignore from git repos
# For files specific to a repo(s), create a separate .gitignore file

######################################################################
# .gitignore rules
# -------------------------------------------------------------------
#  # a comment - this is ignored
#  # no .a files
#  *.a
#  # but do track lib.a, even though you're ignoring .a files above
#  !lib.a
#  # only ignore the root FOO file, not subdir/FOO
#  /FOO
#  # ignore all files in the build/ directory
#  build/
#  # ignore doc/notes.txt, but not doc/server/arch.txt
#  doc/*.txt
#  # ignore all .txt files in the doc/ directory
#  doc/**/*.txt
#--------------------------------------------------------------------- 
######################################################################

# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.out
*.so

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
*.bz2
*.bzip

# These are LaTeX non-source files #
####################################
*.aux
*.log
*.lof
*.lot
*.toc

# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Private or terminal specific files #
######################################
.git-credentials
*/git/credentials

# temporary files #
###################
*.*~
*.~*
*~
*.swp
~*
